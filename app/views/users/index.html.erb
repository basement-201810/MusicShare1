<h1>User一覧</h1>
<table class="table table-hover">
	<thead>
		<th></th>
		<th></th>
		<th></th>
	</thead>
	<tbody>
		<% @users.each do |user| %>
			<tr>
				<td><%= user.name %></td>
				<td><%= user.reviews %></td>
				<td><%= link_to "show", user, class: "btn btn-success" %></td>
			</tr>
		<% end %>
	</tbody>
</table>